<h1>Update your profile</h1>

<%= image_tag @user.image_url %>

<%= form_for(@user) do |f| %>


  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit "Save changes", class: "button" %>
<% end %>
